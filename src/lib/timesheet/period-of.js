// the date strings a pay period covers, in QSP's own mm/dd/yy spelling.
// Re-exported from time-off.js, where the list was first needed - importable
// on its own so a server action can reach it without the rest of that module.
export { periodDates } from "./time-off.js";
